import React, { useEffect, useState } from "react";
import {
  StyleSheet,
  View,
  FlatList,
  Alert,
  TouchableWithoutFeedback,
  Keyboard,
  Text,
} from "react-native";

export default function Dashboard() {
  /**var */
  const module = "Dashboard";
  const moduleLabel = "Dashboard";
  const moduleClass = "c-dashboard";

  return (
    <View>
      <Text>Dashboard</Text>
    </View>
  );
}
